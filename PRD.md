# 한국어 교정기 사용 안내 RAG 챗봇 — PRD

> Main Quest 3(내 도메인 RAG 챗봇 만들기) 제출용. 도메인은 `korean-subtitle-corrector`
> (한국어 띄어쓰기·맞춤법 자동 교정 도구)의 사용법 및 설계 배경이다.

## 1. 문제 정의

이 챗봇은 `korean-subtitle-corrector`를 쓰는 사람이 던지는 두 갈래 질문에 답한다.

1. **사용법형**: "자막이랑 일반 글은 왜 다르게 처리해?", "구두점 표기는 뭘 골라야
   해?", "이 파일 형식은 지원해?" — 화면 안내문·README 기반 질문.
2. **설계배경형**: "왜 사투리는 자동 교정 안 해?", "왜 이 표현은 플래그만 뜨고
   자동으로 안 고쳐져?" — `DESIGN_PRINCIPLES.md`·`KNOWN_LIMITATIONS.md` 등 교정기의
   판단 원칙에 근거해 답한다.

두 갈래 모두 **교정기 저장소 안의 기존 문서**가 근거이며, 챗봇이 새 규범 판단을
만들어내지 않는다 — 문서에 없는 질문에는 "문서에서 못 찾음"으로 답한다.

## 2. 타겟 유저

일반 사용자부터 전문 번역가까지, 이 교정기를 처음 쓰거나 옵션이 헷갈리는 사람
전부. 숙련도 구분 없이 하나의 챗봇으로 대응한다.

**전제(2026-08-28 명시)**: `korean-subtitle-corrector`는 클라우드에 배포하지
않고 로컬 전용으로 운영한다(형태소 분석기 모델이 커서 무료 클라우드 티어
메모리 한계를 넘음, `README.md` 근거). 즉 "여러 사람이 공용으로 접속하는
공개 서버"가 없고, 각자 자기 컴퓨터에서 한 번 서버를 켜고 그 뒤로는 웹페이지
처럼 쓴다. 이 챗봇의 사용법 안내도 이 전제를 그대로 따른다 — "서버를 매번
다시 켜야 한다"거나 "API를 직접 호출해야 한다"처럼 들리면 안 되고, "처음
한 번만 켜 두면 그 다음부터는 웹페이지만 열면 된다"는 것을 분명히 한다.

## 3. 핵심 기능 (MVP)

1. 질문 입력 → 관련 문서 청크 검색 (하이브리드: 코사인 유사도 상위 10 + BM25 상위 5, 중복 제거)
2. 검색된 청크를 근거로 답변 생성 (로컬 Ollama `qwen3.5:2b`, 스트리밍, 취소 가능)
3. 답변에 출처 칩 표시 — 청크 id·섹션·검색 방법(의미/BM25)·점수, 클릭해서 원문 확인
4. 근거가 약하면(원점수 코사인 < 0.55) 검색 결과를 버리지 않고 프롬프트를
   보수화 + UI에 경고 배지 표시 — 모델이 단정 대신 자료의 한계를 말하게 한다
   (하드 차단이 아니다 — `docs/EXPERIMENTS.md` 사이클 3 참고)
5. 답변마다 자동 판정(LLM-as-a-Judge, 6필드: grounded·noHalluc·cited·refusal·
   score·comment) 결과 표시
6. 사람 피드백(👍👎) 남기기, 자동 판정 결과와 나란히 표시
7. Ollama 연결 상태 확인(`GET /api/tags`) — 꺼져 있으면 배너와 재시도 버튼을
   보여 생성 실패와 혼동하지 않게 한다

## 3-1. 비목표 (이번 범위에서 하지 않을 일)

- **로그인·사용자 계정을 만들지 않는다.** 누구나 링크만으로 쓸 수 있어야 한다.
- **전문 데이터베이스 검색이나 서버 수집을 하지 않는다.** 정적 파일 + 브라우저
  검색 + 사용자 로컬 Ollama만으로 완결된다. 서버는 없다.
- **교정기의 새 규범 판단을 만들어내지 않는다.** 챗봇은 교정기 저장소 문서를
  요약·설명할 뿐, 국립국어원 규범을 스스로 해석해 새 답을 만들지 않는다.
- **다국어 지원·모바일 최적화를 이번 범위에 넣지 않는다.**
- **교정기 자체의 기능(실제 교정 실행)을 이 챗봇 안에서 대신하지 않는다.**
  안내만 하고, 실제 교정은 사용자가 교정기 화면에서 한다.

## 3-2. 수용 기준 (검증 가능한 문장으로)

- "자막이랑 일반 글은 왜 다르게 처리해?"에 `[id]` 인용이 포함된 스트리밍
  답변이 표시된다.
- "내일 날씨 어때?" 같은 도메인 밖 질문에는 판정 배지가 `refusal:true,
  grounded:false`로 뜨고, 답변은 자료를 지어내지 않는다(2026-08-28 브라우저
  실측 확인 완료).
- 모든 답변에는 6필드 판정 배지가 있으며, 판정 API 호출이 실패해도("판정
  실패" 배지만 뜸) 이미 생성된 답변과 출처는 화면에서 사라지지 않는다.
- 근거 검색 점수(코사인)가 0.55 미만이면 답변 위에 "약한 근거" 경고가 뜨고,
  그 상태에서도 자료에 없는 내용은 만들어내지 않는다.
- 출처 칩을 누르면 원문 스니펫과 GitHub 원문 링크가 열린다.
- Ollama가 꺼져 있으면 채팅 입력 위에 배너와 "다시 시도" 버튼이 뜨고, 그
  상태를 생성 실패로 오해하게 두지 않는다.
- API 키 없이(로컬 Ollama만으로) 판정까지 전부 동작한다.

## 4. 근거 자료 계획

### 4.1 소스 파일

모두 `korean-subtitle-corrector` 저장소 원본, 출처는 GitHub blob URL
(`github.com/tigermorning/korean-subtitle-corrector/blob/master/<path>#L<시작>-L<끝>`)로 표시한다.

**사용법형 자료**
- `README.md`
- `static/index.html` — 코드가 아닌 사용자 노출 안내문 텍스트만 추출

**설계배경형 자료**
- `docs/DESIGN_PRINCIPLES.md` — 오탐지 4대 원리 (핵심 근거)
- `docs/KNOWN_LIMITATIONS.md` — 구체 오탐지/한계 사례
- `docs/LOANWORD_EXCEPTIONS.md` — 외래어 표기법 예외 규정
- `PRD.md`(교정기 저장소) 중 **사투리 처리 원칙**·핵심 설계 섹션만 (전체 아님)

**제외** — 사용자 관심사가 아니고 자주 바뀌어 벡터스토어를 금방 낡게 만드는 자료
- `PRD.md`(교정기)의 "세션 인계 노트" 전체
- `docs/BACKLOG.md`
- `AGENTS.md`의 커밋 훅·작업 절차 같은 개발자용 규칙

### 4.2 청크 스키마

```json
{"id": "design-principles-원리2", "url": "https://github.com/tigermorning/korean-subtitle-corrector/blob/master/docs/DESIGN_PRINCIPLES.md#L40-L58", "section": "원리 2: 모호할 때 자동 교정 금지", "text": "...", "vector": [0.01, -0.02, ...]}
```

- `id`: 파일-섹션 slug (챗봇이 답변 안에서 이 id를 그대로 인용한다)
- `url`: 줄 범위를 포함한 GitHub blob URL (원문 확인용)
- `section`: 마크다운 헤딩 경로
- `text`: 사실 단위 1개 — 판단 하나·설명 하나당 청크 하나
- `vector`: `onnx-community/embeddinggemma-300m-ONNX`(dtype `q4`)로 만든 768차원
  임베딩. 문서 쪽 프롬프트는 `"title: {section} | text: {text}"`(공식 템플릿),
  질문 쪽은 `"task: search result | query: {question}"` — 이 접두사 규칙이
  깨지면 코사인 유사도 자체가 의미를 잃는다(`scripts/build_vectorstore.mjs`,
  `app.js`가 각각 문서/질문 쪽을 맡는다).

정적 파일은 `data/vectorstore.json`(id·url·section·text·vector 배열)이고,
`npm run build:vectorstore`로 재생성한다.

## 5. 화면 구성

1. **메인 채팅 화면** — 질문 입력창, 스트리밍 답변 출력, 취소 버튼
2. **출처 표시** — 답변 하단 출처 칩(id·섹션·검색 방법·점수), 클릭 시 원문 스니펫 펼침
3. **판정·피드백 영역** — 자동 판정 배지(6필드) + 약한 근거 경고, 사람 피드백 버튼(👍👎)
4. **엔진 로딩 안내** — 임베딩 모델(약 200MB, 첫 방문 1회) 다운로드 진행률 표시
5. **Ollama 상태 배너** — 연결 끊기면 배너+재시도 버튼, 생성 실패와 구분

실험 결과(세팅별 성능 비교표)는 별도 화면 없이 이 저장소의 README/PRD 문서에
표로 남긴다.
